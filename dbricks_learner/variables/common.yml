variables:
  prename:
    description: Predix name
    default: dev

  catalog:
    default: poc_dev
    description: The catalog to use

  schema:
    default: ${workspace.current_user.short_name}
    description: The schema to use
